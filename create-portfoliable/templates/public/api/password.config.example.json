{
  "_comment": "Copy to public/api/password.config.json for self-hosted PHP unlock verification. Keep each case password unique.",
  "_format": {
    "description": "Map case id to one password_hash() record.",
    "algorithm": "password_hash",
    "recommended": "PASSWORD_ARGON2ID"
  },
  "cases": {
    "mobile-product-launch": {
      "hash": "$argon2id$v=19$m=65536,t=4,p=1$replace$with-generated-hash"
    },
    "mobile-checkout-flow": {
      "hash": "$argon2id$v=19$m=65536,t=4,p=1$replace$with-generated-hash"
    }
  }
}
